void main(){
  /*
  null
  - kata kunci null merupakan tipe data representasi data kosong
  - secara default, kita membuat variable, data harus diisi, jika tidak diisi, maka variable tidak bisa di gunakan
  - namun jika kita ingin membuat sebuah variable bisa diisi data null, kita bisa tambahkan karakter ?(tanda tanya) seperti ini
  tipeData ? namaVariable = null;
   */
  int number1;
  int? number2;

  // print(number1); //error
  print(number2);
}